 #Check length of both the strings are same
    #if the count of any char not equal in both then return False
         
        if len(s) == len(t):
                for i in s:
                     if s.count(i) != t.count(i):
                            return False
                return True
        
